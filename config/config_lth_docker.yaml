sap:
  base_url: "https://20.85.79.25:50000/b1s/v1"
  company_db: "TAJIBOS_QA"
  username: "manager"
  password: "AddProd25$"
  session_timeout_minutes: 30
  verify_ssl: false

paths:
  inbound:   "/data/lth/inbound"
  processed: "/data/lth/procesados"
  errors:    "/data/lth/errores"
  reports:   "/data/lth/reportes"

scheduler:
  interval_seconds: 60

concurrency:
  max_workers: 6

accounts:
  cash_account:      "_SYS00000000024"
  transfer_account:  "_SYS00000000035"
  default_card_code: 1
  credit_card_account: ""

retry:
  max_attempts: 3
  backoff_seconds: 5

idempotency:
  enabled: false
  reference_field: "U_RefBanco"

logging:
  level: "INFO"
  file: ""